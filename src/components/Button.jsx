const Button = ({text}) => {
  return <button className="px-8 py-4 bg-blue-500 rounded-md font-semibold text-white hover:bg-blue-600 active:scale-105 transition-all">{text}</button>;
};

export default Button;
